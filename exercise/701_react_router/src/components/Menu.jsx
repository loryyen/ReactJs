require('./menu.scss');

var Link = ReactRouter.Link;

var Menu = function(props){
  return (
    <div className='menu'>
      {props.pages.map(function(page){
        return (
          // code here...







        );
      })}
    </div>
  );
};
module.exports = Menu;
